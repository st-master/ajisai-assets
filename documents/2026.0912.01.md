---
title: "Git push拒否（non-fast-forward）の対処法"
category: "Git・開発メモ"
date: 2026-09-12
---

### Q. git pushしたら non-fast-forward エラーが出た場合の対処は？

---

### A. git pull --rebase を実行する

リモートの変更がローカルより先行しているため、`git pull --rebase origin main` で変更を取り込んでから `git push` を行います。