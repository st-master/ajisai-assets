---
type: md
title: マイ・ポータル・ログ魂の航海記
date: 2026.0917
category: Blog
tags: 
- blog
- template
- python
---

# マイ・ポータル・ログ魂の航海記 — 資産管理マネージャー構築の全記録 —

全体のルートマネージャーから独立し、`assets/` ディレクトリ配下を完全に一元管理する「資産管理マネージャー (`build.py`)」と、それを支える「汎用テンプレート (`template.html`)」の構築に至る思考と実装の航海記。

---

## 1. 航海の始まり：資産管理マネージャーの独立構想

全体のポータルを司るルート直下の `build.py` とは別に、静的資産（画像・動画・音声）の管理だけに特化した**資産管理マネージャー**を `assets/` 直下に設ける着想からスタートした。

* **目的:** 静的リソースの自動ギャラリー化と、他の環境へ丸ごと移植できるポータビリティの確保。
* **ソート順の厳格化:** ファイル名規則（`yyyy.mmdd.nn`）に基づき、常に最新のアセットが上にくる「降順（`reverse=True`）」ソートを採用。

```python
# 降順ソートによる新着順カード生成
for filename in sorted(os.listdir(images_dir), reverse=True):
    ...
    